<resources xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <!-- Base application theme. -->
    <style name="Theme.MasterMind" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <!-- status bar -->
        <item name="colorPrimaryVariant">@color/black</item>
        <!-- button text color -->
        <item name="colorOnPrimary">@color/textcolor</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/black</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- settings textcolor -->
        <item name="android:textColor">@color/textcolor</item>
        <item name="android:textColorPrimary">@color/textcolor</item>
        <item name="android:textColorSecondary">@color/textcolor</item>
        <item name="android:textColorTertiary">@color/textcolor</item>
        <!-- preferences colors -->
        <!-- preference titel color -->
        <item name="android:colorAccent">@color/textcolor</item>
        <!-- swtitch preference activated color, slider preference color -->
        <item name="colorControlActivated">@color/textcolor</item>

        <!-- font -->
        <item name="android:fontFamily">@font/general_font</item>
    </style>

</resources>